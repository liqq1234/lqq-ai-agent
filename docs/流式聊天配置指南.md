# 流式聊天功能配置指南

## ✅ 已完成的修改

### 后端修改（需要重启）

#### 1. ChatController.java
**文件**: `lqq-chat/src/main/java/com/lqq/lqqaiagent/controller/ChatController.java`

**修改内容**：
- 流式接口 `/chat/stream` 同时支持 GET 和 POST 请求
- GET 请求通过 `?message=xxx` 传参
- POST 请求通过 JSON body 传参

```java
@RequestMapping(value = "/stream", method = {RequestMethod.GET, RequestMethod.POST}, 
                produces = MediaType.TEXT_EVENT_STREAM_VALUE)
public Flux<String> streamChat(
        @RequestParam(value = "message", required = false) String messageParam,
        @RequestBody(required = false) Map<String, String> request) {
    // 优先使用 GET 参数，否则使用 POST body
    String message = messageParam != null ? messageParam : 
                    (request != null ? request.get("message") : null);
    // ...
}
```

#### 2. pom.xml
**文件**: `lqq-bootstrap/pom.xml`

**修改内容**：
- 添加 `lqq-chat` 模块依赖，使 ChatController 被扫描到

```xml
<dependency>
    <groupId>com.lqq</groupId>
    <artifactId>lqq-chat</artifactId>
    <version>0.0.1-SNAPSHOT</version>
</dependency>
```

---

### 前端修改（已自动生效）

#### 1. ChatInterface.vue
**文件**: `src/views/ChatInterface.vue`

**修改内容**：
1. 导入 `connectSSE` 函数
2. 修改 `handleSend` 函数，使用流式聊天接口
3. 实时累积和显示 AI 回复

**关键代码**：
```javascript
// 使用流式聊天
let fullResponse = ''

const eventSource = connectSSE(
  '/chat/stream',
  { message }, // GET 查询参数
  (data) => {
    if (data === '[DONE]') {
      console.log('✅ 流式聊天完成')
      chatStore.setLoading(false)
    } else {
      fullResponse += data
      chatStore.updateLastMessage(fullResponse) // 实时更新
    }
  },
  (error) => {
    console.error('❌ 流式聊天失败:', error)
    chatStore.updateLastMessage(`抱歉，发生了错误: 连接失败`)
  }
)
```

---

## 🔄 重启服务

### 必须重启后端（关键）

#### 方法 1：在 IDE 中
1. 停止 `LqqAiAgentApplication` (端口 8224)
2. 等待完全停止
3. 重新运行 `LqqAiAgentApplication`

#### 方法 2：使用命令行
```bash
cd c:\Users\LQQ\Workspace\IDEASpace\lqq-ai-agent

# 清理并重新编译
mvn clean install -DskipTests

# 重新启动
mvn spring-boot:run -pl lqq-bootstrap
```

### 前端（Vite 自动热重载）
前端代码修改后会自动热重载，无需手动重启。如果没有生效：
```bash
# 停止前端 (Ctrl + C)
# 重新启动
npm run dev
```

---

## ✅ 验证流式聊天

### 1. 检查后端日志

重启后，查看日志应该包含：
```
Mapped "{[/chat/stream],methods=[GET,POST]}" onto ...ChatController.streamChat
```

### 2. 测试流式接口

在浏览器开发者工具中测试：

```javascript
// 创建 EventSource 连接
const es = new EventSource('http://localhost:8224/api/chat/stream?message=你好')

es.onmessage = (event) => {
  console.log('收到:', event.data)
  if (event.data === '[DONE]') {
    console.log('完成')
    es.close()
  }
}

es.onerror = (error) => {
  console.error('错误:', error)
  es.close()
}
```

**预期输出**：
```
收到: 你好
收到: ！
收到: 我是
收到: Qwen
收到: ，
收到: 很高兴
收到: 为您
收到: 服务
收到: [DONE]
完成
```

### 3. 前端测试

1. 打开前端页面（http://localhost:3000）
2. 在聊天框输入消息
3. 观察 AI 回复是否**逐字显示**（而不是一次性显示）

---

## 🎯 效果对比

### ❌ 修复前（非流式）
- AI 回复一次性显示
- 显示原始 JSON 格式：`{"message": "...", "timestamp": ...}`
- 等待时间长，体验差

### ✅ 修复后（流式）
- AI 回复逐字显示（打字机效果）
- 只显示 AI 的回复内容
- 实时反馈，体验好

---

## 📋 完整检查清单

- [x] **后端**: `ChatController.java` 支持 GET 请求
- [x] **后端**: `pom.xml` 添加 `lqq-chat` 依赖
- [x] **前端**: `ChatInterface.vue` 使用流式接口
- [ ] **重启后端服务**（必须）
- [ ] 测试流式接口可用
- [ ] 前端发送消息，观察逐字显示

---

## 🚨 常见问题

### Q1: 前端报错 "Failed to construct 'EventSource'"
**原因**: 后端未重启，接口不支持 GET 请求

**解决**: 重启后端服务

---

### Q2: AI 回复不是逐字显示
**原因**: 前端代码未生效或后端返回格式错误

**解决**:
1. 检查浏览器控制台日志
2. 确认使用的是 `/chat/stream` 接口
3. 检查后端日志是否有 `onPartialResponse` 输出

---

### Q3: 后端日志报错 "streamingChatModel is null"
**原因**: Ollama 服务未运行或模型未下载

**解决**:
```bash
# 检查 Ollama 服务
netstat -ano | findstr :11434

# 下载模型
ollama pull qwen2.5:7b

# 查看已安装的模型
ollama list
```

---

## 🎓 技术要点（面试重点）

### 1. SSE (Server-Sent Events)
- **定义**: 服务器主动向浏览器推送数据的技术
- **特点**: 单向通信（服务器→客户端）、HTTP 协议、自动重连
- **与 WebSocket 区别**: WebSocket 是双向通信，SSE 更轻量

### 2. Reactor (响应式编程)
- **Flux**: 表示 0-N 个元素的异步序列
- **Sinks**: 手动推送数据到 Flux
- **背压**: 自动控制数据流速度

### 3. LangChain4j 流式输出
- **StreamingChatModel**: 流式聊天模型接口
- **StreamingChatResponseHandler**: 处理流式响应的回调
- **onPartialResponse**: 每次接收到部分响应时触发

### 4. 前端流式处理
- **EventSource**: HTML5 原生 SSE 客户端
- **累积显示**: 实时累加每个片段，更新 UI

---

## 📝 相关文件

### 后端
- ✅ `ChatController.java` - 流式聊天接口
- ✅ `pom.xml` - 模块依赖
- ✅ `application.yml` - LangChain4j 配置

### 前端
- ✅ `ChatInterface.vue` - 聊天主界面
- ✅ `api/index.js` - SSE 连接工具
- ✅ `.env.development` - API 基础 URL

---

## 🎉 完成标志

当你看到 AI 回复**一个字一个字地出现**（像打字机一样），就说明流式聊天配置成功了！

---

**最后更新**: 2025-11-22 21:40
**修改人员**: Cascade AI
